[project]
name = "backend"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "fastapi[standard,standard-no-fastapi-cloud-cli]>=0.141.1",
    "pillow>=12.3.0",
    "torch>=2.13.0",
    "torchvision>=0.28.0",
]
